<view class="container">
  <view class='content'>
    <view class='box clearfix' wx:for="{{ data }}" wx:key="{{ resourceBillId }}" >
      <view class='box_title'>{{ item.address }}</view>
      <view class='box_text'>
        <text class='text'>{{ item.categoryName }}</text>
        <text class='text'>{{ item.categoryName }}</text>
        <text class='text'>{{ item.categoryName }}</text>  
        <view class='del' data-index='index' bindtap='delTap'>删除</view>
      </view>
      <view class='price'>没这数据</view>
    <view class='shopping' bindtap='proListTap'>立即购买</view>
    </view>
  </view>
</view>
